{%- assign categories_enable 	= section.settings.categories_enable -%}
{%- assign categories_title 	= section.settings.categories_title -%}
{%- assign show_count		 	= section.settings.show_count -%}
{%- if categories_enable -%}
	<div class="sidebar-categories js-filter filter-list">
		<h4 class="block-sidebar__title">{{ categories_title }}</h4>
		{%- include 'collection-categories' -%}
	</div>
{%- endif -%}
{%- if section.settings.enable_filtering -%}
	{% render 'filter-sidebar', results: collection, enable_filtering: section.settings.enable_filtering %}
{%- endif -%}
{% schema %}
  {
    "name": "Sidebar top",
    "settings": [
      {
        "type": "header",
        "content": "Collection"
      },
      {
        "type": "checkbox",
        "id": "categories_enable",
        "label": "Enable",
        "default": true
      },
      {
        "type": "text",
        "id": "categories_title",
        "label": "Heading",
        "default": "Collection"
      },
	  {
      "type": "link_list",
      "id": "categories_menu",
      "label": "Menu"
    },
	  {
        "type": "checkbox",
        "id": "show_count",
        "label": "Show Count",
        "default": true
      },
	   {
        "type": "header",
        "content": "Filter"
      },
      {
        "type": "checkbox",
        "id": "enable_filtering",
        "label": "Enable"
      }
    ]
  }
{% endschema %}

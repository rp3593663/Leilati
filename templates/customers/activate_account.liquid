<section class="page-account">
  <div class="container">
    <div class="page-account__inner activate-account">
      <header class="page-header">
        <h1 class="page-title">{{ 'customer.activate_account.title' | t }}</h1>
        <p>{{ 'customer.activate_account.subtext' | t }}</p>
      </header>
      <div class="activate-account__form">
        {% form 'activate_customer_password' %}
          {{ form.errors | default_errors }}
          <div class="form-group form-group--pasword">
            <label class="d-none" for="customerPassword">{{ 'customer.activate_account.password' | t }}</label>
            <input id="customerPassword"
              class="form-control{% if form.errors contains 'password' %} is-invalid{% endif %}"
              name="customer[password]"
              type="password"
              placeholder="{{ 'customer.activate_account.password' | t }}"
              required/>
            <a href="javascript:void(0)" class="js-password-toggle password-toggle">
              <span class="text-show-password"><i class="feather-eye"></i></span>
              <span class="text-hide-password"><i class="feather-eye-off"></i></span>
            </a>
          </div>
          <div class="form-group form-group--pasword">
            <label class="d-none" for="customerPasswordConfirmation">{{ 'customer.activate_account.password_confirm' | t }}</label>
            <input id="customerPasswordConfirmation"
              class="form-control{% if form.errors contains 'password_confirmation' %} is-invalid{% endif %}"
              name="customer[password_confirmation]" 
              type="password"
              placeholder="{{ 'customer.activate_account.password_confirm' | t }}"
              required/>
            <a href="javascript:void(0)" class="js-password-toggle password-toggle">
              <span class="text-show-password"><i class="feather-eye"></i></span>
              <span class="text-hide-password"><i class="feather-eye-off"></i></span>
            </a>
          </div>
          <div class="form-button">
            <input type="submit" class="btn btn-primary" value="{{ 'customer.activate_account.submit' | t }}">
          </div>
          <div class="form-button">
            <input type="submit" class="btn btn-secondary" name="decline" value="{{ 'customer.activate_account.cancel' | t }}">
          </div>
        {% endform %}
      </div>
    </div>
  </div>
</section>
